@model IEnumerable<Cap.Domain.Models.Cap.GrupoFinanceiro>

@{
    ViewBag.Title = "Grupos financeiros";
}

<h2>Grupos financeiros</h2>

<p>
    @Html.ActionLink("Novo grupo financeiro", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ativo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ativo)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.Id }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.Id }) |
            @Html.ActionLink("Contas financeiras", "Index", "ContaFinanceira", new { id=item.Id }, null)
        </td>
    </tr>
}

</table>
